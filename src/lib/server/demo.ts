// Demo mode: logged-in users who aren't whitelisted get a read-only preview of a
// single showcase day. Nothing they do is persisted to the DB, CDN, or the album.
export const DEMO_DAY = '2026-07-02';
